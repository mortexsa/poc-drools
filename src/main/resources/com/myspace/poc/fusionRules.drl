package com.myspace.poc;

import com.myspace.poc.Event;


rule "Sound the alarm"
when
    $e  : Event( ) from entry-point "DEFAULT"
    $ea : Event( this after[4s,50s] $e ) from entry-point "DEFAULT"
then
    $ea.setMessage("j'ai bien recu le message");
end