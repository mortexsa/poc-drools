package com.myspace.poc;

import com.myspace.poc.Event;
import com.myspace.poc.User;

declare User
    @role( event )
end

rule "Sound the alarm"
when
    $e : Event( )
    not( User( this after[12s,15s] $e ) )
then
    System.out.println("jai bien recu levent a hbibi: "+$e.getMessage());
end