package com.myspace.poc;

import com.myspace.poc.Event;
import com.myspace.poc.User;


rule "Sound the alarm"
when
    $e : Event( )
    $u : User( this after[4s,50s] $e )
then
    System.out.println("jai bien recu levent a hbibi: "+$u.getName());
end