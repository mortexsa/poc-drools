package com.myspace.poc;

import com.myspace.poc.Event;
import com.myspace.poc.Risk;


rule "Sound the alarm"
when
    $r  : Risk( )
    $e  : Event( ) from entry-point "DEFAULT"
    $e2 : Event( this after[4s,50s] $e ) from entry-point "DEFAULT"
then
    $r.setId(1L);
    $r.setValue(100);
end